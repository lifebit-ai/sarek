/*
 * -------------------------------------------------
 *  Nextflow config file for running on a local machine eg. laptop
 * -------------------------------------------------
 */

params {
    executor = 'local'
}
